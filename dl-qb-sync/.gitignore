node_modules/
dist/
.env
server/db/*.sqlite
server/db/*.sqlite-*

# Excel con datos reales de pacientes/comisiones, solo se usa localmente para
# el seed de una sola vez -- nunca debe subirse a un repo de Git.
public/*.xlsx
public/~$*
